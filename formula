<meta charset="utf-8">
<script>
    
var x = prompt(`Введите число Х:`);
var a = 2;
var b = -1;
var c = 4;
var y = a * (x ^ 2) + b * x + c;

alert (`При х="` + x + `" формула "y=(` + a + `)*x^2+(` + b + `)*x+(`+ c + "), равна " + y);

</script>
